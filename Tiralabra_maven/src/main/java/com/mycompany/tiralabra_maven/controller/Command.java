

package com.mycompany.tiralabra_maven.controller;

/**
 * Provides an interface for the command as specified in the command design pattern.
 * Classes implementing the command interface are expected to declare the execute method. 
 * @author gabriel
 */
public interface Command {
    void execute();
}
